import navStyles from "./NavBar.module.css";

const NavBar = () => {
    return (
        <div className={navStyles.Container}>
            <div>Log</div>
            <div>LOGIN</div>
        </div>
    );
};

export default NavBar;
